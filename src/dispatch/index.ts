/** What the rest of the app reaches the Dispatch tab by: the left sidebar, the dashboard and the
 *  notifications (`child-routing`) glob this file. It imports none of them. */
export { openDispatch, parentWorktree, useWaveLines } from './live'
export { ISSUES, type WaveLine } from './model'
